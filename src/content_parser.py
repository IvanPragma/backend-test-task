from __future__ import annotations


def parse_useful_content(url: str, parse_images: bool) -> str:
    """Parse useful site content by url."""

    return f'This like useful content for url "{url}"'
